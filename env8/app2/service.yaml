apiVersion: v1
kind: Service
metadata:
  name: my-app-env8-app2
  labels:
    app: my-app-env8-app2
spec:
  selector:
    app: my-app
    matchLabels:
      app: null-env8-app2
  ports:
    - port: 80
      targetPort: 80
  template:
    metadata:
      labels:
        app: null-env8-app2
