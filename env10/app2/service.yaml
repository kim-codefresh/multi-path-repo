apiVersion: v1
kind: Service
metadata:
  name: my-app-env10-app2
  labels:
    app: my-app-env10-app2
spec:
  selector:
    app: my-app
    matchLabels:
      app: null-env10-app2
  ports:
    - port: 80
      targetPort: 80
  template:
    metadata:
      labels:
        app: null-env10-app2
